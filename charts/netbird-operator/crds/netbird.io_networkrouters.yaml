---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: networkrouters.netbird.io
spec:
  group: netbird.io
  names:
    kind: NetworkRouter
    listKind: NetworkRouterList
    plural: networkrouters
    singular: networkrouter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.networkRef.name
      name: Network
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkRouter is the Schema for the networkrouters API: a NetBird router (a
          peer group bound to a network) plus its routing-peer source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NetworkRouterSpec mirrors the NetBird router API
              (POST /api/networks/{network}/routers) and adds the routing-peer source: an
              existing NetBird group, or a netbird-client DaemonSet the operator deploys.
            properties:
              enabled:
                default: true
                description: Enabled controls whether the router is active.
                type: boolean
              masquerade:
                default: true
                description: Masquerade makes the routing peers SNAT traffic to the
                  routed resources.
                type: boolean
              metric:
                default: 9999
                description: Metric is the route metric; the lowest number wins.
                maximum: 9999
                minimum: 1
                type: integer
              networkRef:
                description: |-
                  NetworkRef references the Network this router belongs to. The Network must
                  be Ready; its status.networkID identifies the NetBird network.
                properties:
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: Namespace of the referent.
                    type: string
                required:
                - name
                - namespace
                type: object
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
              peers:
                description: Peers selects the routing peers — exactly one of group
                  or deploy.
                properties:
                  deploy:
                    description: |-
                      Deploy runs a hostNetwork DaemonSet of netbird-client as the routing peers;
                      the operator manages its Group, SetupKey and DaemonSet.
                    properties:
                      image:
                        description: |-
                          Image overrides the netbird-client image (default: the operator's
                          configured client image).
                        type: string
                      logLevel:
                        description: LogLevel for the netbird client.
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        - trace
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: 'NodeSelector limits the DaemonSet to matching
                          nodes (default: all nodes).'
                        type: object
                    type: object
                  group:
                    description: |-
                      Group reuses an existing NetBird group as the routing peers (e.g. the group
                      the host-level netbird on the cluster nodes auto-joins). The operator
                      creates only the router and deploys nothing.
                    properties:
                      id:
                        description: ID is the id of the group.
                        type: string
                      localRef:
                        description: LocalReference is a reference to a group in the
                          same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      name:
                        description: Name is the name of the group.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: Exactly one of id, name, or localRef must be set
                      rule: (has(self.id)?1:0)+(has(self.name)?1:0)+(has(self.localRef)?1:0)==1
                type: object
                x-kubernetes-validations:
                - message: exactly one of peers.group or peers.deploy must be set
                  rule: has(self.group) != has(self.deploy)
            required:
            - networkRef
            - peers
            type: object
          status:
            default:
              observedGeneration: -1
            description: NetworkRouterStatus defines the observed state of NetworkRouter.
            properties:
              conditions:
                description: Conditions holds the conditions for the NetworkRouter.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              groupID:
                description: GroupID is the id of the peer group bound to the router.
                type: string
              networkID:
                description: NetworkID is the id of the network the router is created
                  in.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation.
                format: int64
                type: integer
              routerID:
                description: RouterID is the id of the created NetBird router.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
